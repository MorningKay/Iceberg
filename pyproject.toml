[project]
name = "iceberg"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10,<3.11"
dependencies = []

[dependency-groups]
classifier = [
    "accelerate>=1.12.0",
    "datasets>=4.5.0",
    "evaluate>=0.4.6",
    "pyyaml>=6.0.3",
    "rich>=14.3.1",
    "scikit-learn>=1.7.2",
    "tqdm>=4.67.1",
    "transformers>=5.0.0",
]
dev = [
    "pytest>=9.0.2",
    "ruff>=0.14.14",
]
